diff --git a/dist/index.js b/dist/index.js
index 513e29101c3ba65d503bc6d823071dc7f1cf30d8..45d1c0d381f19708f0ca9f09788a06dbadc91acd 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -666,9 +666,9 @@ function applyCoverage(prepared, ranges) {
     return data;
 }
 function applyCoverageToMap(coverageMap, prepared, ranges) {
-    const data = {};
-    for (const [filename, template] of Object.entries(prepared.files))data[filename] = getOrCreateFileCoverage(coverageMap, filename, template);
-    applyCoverageHits(data, prepared, ranges);
+    // Different compiled modules can map to the same Vue source with different indexes.
+    // Istanbul merges by source locations; reuse of the first module's indexes cannot.
+    coverageMap.merge(applyCoverage(prepared, ranges));
 }
 function createFileCoverage(template) {
     const fileCoverage = {
