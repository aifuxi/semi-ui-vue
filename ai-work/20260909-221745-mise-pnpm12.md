# AI 工作记录：mise 与 pnpm v12 迁移

- 日期：2026-09-09
- 状态：完成（工具链迁移与验证；全量门禁未全部通过）
- 工作分支：`codex/mise-pnpm12`
- 隔离位置：`.worktrees/mise-pnpm12`
- 起点：`77dc7d8`

## 目标与验收

使用 mise 管理 Node 24.18.0 / pnpm 12.3.4，本地与 CI 共用精确版本；保留依赖版本、workspace 边界和用户已有服务。验证干净安装、frozen install、工具版本一致性、质量门禁、发布包和浏览器行为。迁移只提交隔离分支，不自动合入或发布。

## 关键决策

- npm 官方 registry 查询时 `latest` 为 12.3.4，`next-12` 为 12.4.0，采用 latest 通道的精确版本。
- 新增 `mise.toml`，删除重复的 `.nvmrc` / `.node-version`；保留 packageManager 与 engines，新增 `check:toolchain` 校验声明和实际版本。
- 使用 `pmOnFail: error`，由 mise 提供工具，pnpm 版本不符时失败；移除旧 `onlyBuiltDependencies`，保留同等 `allowBuilds.esbuild` 策略。
- 原依赖锁文件原文完整保留，pnpm v12 仅在前面新增包管理器依赖 YAML 文档。实际 store 仍是用户级 store/v11，没有生成项目内 store。
- 三个 GitHub workflow 使用固定 SHA 的 mise-action v4，mise 固定 2026.9.3；发布继续使用 npm 11.16.0、显式官方 registry 和 OIDC 权限。没有执行实际发布。
- 发布脚本直接调用 `npm_execpath` 指向的 pnpm 二进制，取消 `node <pnpm binary>` 的错误假设；真实包验证原本已采用直接调用。
- 主目录与迁移分支均忽略 `.worktrees/`，同时为 ESLint/Prettier 添加扫描排除。主目录忽略规则单独提交为 `d184f7e`。
- 嵌套 worktree 暴露 inventory 生成器 `resolveConfig(workspaceRoot)` 读取父目录配置的问题。将 inventory 格式明确固定为原来的 80 列，JSON/Markdown 生成物不变。
- 补齐依赖边界检查遗漏的参考页头精确开发依赖 `@douyinfe/semi-site-header@0.0.29`。依据为已存在的 `docs/documentation/site-header.md`；没有新增依赖或扩大公开包边界。
- 主工作区已有 4321 文档服务。文档 Playwright 配置支持 `DOCS_PORT`，手工 BrowserContext 与网络来源断言跟随 baseURL，使用 14321 验收隔离产物。

## 验证证据

- mise 安装 pnpm 12.3.4，GitHub artifact attestations 与 checksum 验证通过。
- `mise exec -- pnpm install --frozen-lockfile` 首次报告缺少包管理器锁记录；一次 `--no-frozen-lockfile` 安装通过，随后 frozen install 通过。
- 对照 Git 原锁文件，业务依赖部分逐字一致。
- `pnpm check:toolchain` 通过；临时 fixture 验证一致配置成功、pnpm 声明不一致时拒绝。
- 三份 workflow YAML 解析和 mise 初始化检查通过。GitHub Actions 本身尚未运行。
- vendor、inventory、图标、插画、Locale、依赖边界、Vitest aliases 与 coverage exemptions 检查通过。
- 全仓 `pnpm lint` 通过；本次改动文件格式与 `git diff --check` 通过。
- `pnpm test:coverage`：176 个文件、1213 项单测全部通过；coverage threshold 未通过，不能把该命令记为成功。主工作区使用原 pnpm 执行相同 coverage 检查，152 条 threshold 失败项与隔离分支逐项完全相同。
- `DOCS_PORT=14321 DOCS_ACCEPTANCE=1 pnpm --filter @workspace/docs exec playwright test -c playwright.config.ts --fail-on-flaky-tests`：467 passed、8 failed（全部 TreeSelect）、1 flaky（ConfigProvider Direction zh-cn dark rtl），耗时 22.0 分钟。端口适配涉及的 header/portal 用例全部通过。
- `pnpm --filter @workspace/docs build`、`check:dist`、`exec nuxt typecheck` 通过；198 页静态产物、搜索、历史入口、本地 REPL 与许可散列检查通过。
- `pnpm verify:ssr-dist`、`pnpm verify:pack-dist`、`pnpm release:verify` 均以退出码 0 完成：5 个公开包、真实 tarball 安装/类型/样式/SSR/Worker 验证通过。
- `pnpm release:preflight` 按预期在非发布工作区被“工作区和暂存区完全干净”条件拒绝，未进入认证或发布动作。

## 已确认的基线问题

- `pnpm check:full` 在格式检查失败：`ai-work/20260906-114000-config-provider-consumers-verification.md`。主工作区使用原工具链复现同一失败；不改写无关记录。
- `pnpm typecheck:clean` 在 AIChatInput.test.ts:47（unknown）和 Transfer.vue:375 / TransferNodeRenderer.ts:33（VNodeChild 参数）失败。主工作区 UI typecheck 复现同样三项错误。
- TreeSelect 文档受控示例未出现控件：在独立 Chromium 中分别读取隔离静态服务（14321）和主工作区迁移前的静态产物（临时 14322），对应 preview 都是 `<!---->`，主工作区 combobox 数为 0。没有页面异常；临时复核服务已关闭，原 4321 服务保留。
- `pnpm build` 的 parity-vue 类型检查因同样的 Transfer 错误失败，并中断并行 docs 构建；随后单独执行文档构建以继续产物验证。

## 回退与未验证事项

回退迁移提交即可恢复旧工具声明、锁文件及调用方式；重新安装对应依赖。主工作区的 `.worktrees/` 忽略规则可独立保留。没有卸载全局工具、修改个人 shell/IDE 设置或停止主项目服务。

组件浏览器首次开发模式运行：437 通过、5 失败；失败包括 Vite 504 Outdated Optimize Dep、动态导入失败、页面重载及一个参考标识等待超时。原始日志与 trace 已保留，随后执行 `mise exec -- pnpm test:browser:built --fail-on-flaky-tests`，442 项全部通过，退出码 0，耗时 1.9 分钟。预构建模式消除了首轮开发服务重新优化造成的干扰，未修改截图或行为断言。

不声称完整门禁通过。
