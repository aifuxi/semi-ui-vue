# 全量门禁问题修复

## 目标与范围

修复 `20260909-221745-mise-pnpm12.md` 记录的全量门禁问题。起点为 `8440e5f`，工作区初始干净；固定 vendor 提交为 `cdfba6e520fc83ad871b30f51f36d8af3aaa5a21`（v2.102.0）。不降低 coverage 或视觉阈值、不扩大豁免、不增加 retries。

## 已定位问题

- 格式：历史 ConfigProvider 工作记录未通过 Prettier，已仅调整格式。
- 类型：AIChatInput 测试的 mock context 为 unknown；Transfer 两处 `h()` children 参数不接受完整的 VNodeChild 联合。使用实例类型守卫及 children 数组，保留公开返回类型。
- ConfigProvider：旧 trace 的 Modal 比较发生在不同动画状态，React 保留 `semi-modal-content-animate-show` / identity transform，Vue 已进入终态。测试显式完成两侧 CSS 入场动画并等待动画 class 移除，再保持原有 class、computed style、几何、像素断言。
- TreeSelect：本次在原静态产物及重新构建后的静态产物均未复现旧记录的空白。两次 8 项文档用例均无重试通过，不将未复现写成源码修复。
- coverage：初始重跑仍为 176 文件、1213 项单测通过，但命令因 152 条阈值错误退出 1；40 个未达标文件共 1271 个未覆盖分支。继续按公开行为补齐，不能用单测通过替代 coverage 门禁通过。

## 验证记录

- UI typecheck：失败前精确复现三处错误；修复后退出 0。
- AIChatInput / Transfer 定向单测：20 项通过；后续 Transfer 增加 5 种公开渲染返回值回归，Transfer 单元及 SSR 套件通过。
- 全仓 lint：退出 0；修改文件 IDE 诊断未发现 error。
- `pnpm build` 与 `pnpm typecheck`：各包日志均输出 Done，未出现失败；IDE 终端提前超时，未取得最终退出码，因此不能把这两次调用记录为已核实退出码的成功门禁。后续长验证改用可轮询终端。
- TreeSelect 初始定向：8 passed，retries=0。
- ConfigProvider 旧用例定位复跑：目标 zh-cn/dark/rtl 连续 3 次通过，retries=0；旧 trace 提供竞态证据。
- 修复后新静态产物：TreeSelect 8 项加 ConfigProvider 目标用例，共 9 passed，retries=0。
- ConfigProvider 完整双语、明暗、方向矩阵：16 passed，`--fail-on-flaky-tests`，退出 0。
- `pnpm test:browser:built tests/browser/components/transfer.spec.ts --fail-on-flaky-tests`：脚本自身已包含 `tests/browser`，实际选择全仓 442 项，全部通过，退出 0；这不是仅 Transfer 的定向结果。
- `pnpm verify:pack-dist`：真实 tarball 安装、exports、ESM、类型、样式、SSR import 与 JsonViewer Worker 搜索替换全部通过，退出 0。
- Worker manager 补测请求参数、乱序/错误/重复响应、namespace 复用与幂等销毁：4 项通过，定向 statements/branches/functions/lines 均为 100%，退出 0；未改变运行时或阈值。
- 首批提交：`45c1dde`。全量 coverage 复测为 176 文件、1221 项单测通过，阈值错误从 152 降至 148，命令仍退出 1。
- 随后补齐 ConfigProvider locale 缺少 Typography 时的默认文案回退断言，沿用公开复制行为；9 项定向测试通过，ConfigProvider.vue 四项覆盖率均为 100%，退出 0。此后未再次运行全量 coverage，不把定向结果冒充全仓通过。

原始本轮日志位于 `/tmp/semi-gates-*.log`，为临时诊断文件；本记录保存可持久复核的命令、结果和限制。后续验证及剩余项随实际结果更新。

## 当前状态

首批修复已完成并提交；全量任务仍进行中。类型、格式、ConfigProvider 动画采样和 Worker manager 覆盖问题已有验证。TreeSelect 的旧空白未复现。剩余 coverage 欠账及最终 `check:full` 验证仍待完成。
