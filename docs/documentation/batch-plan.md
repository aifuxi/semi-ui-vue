# 文档验收队列

以固定 v2.102.0 的 859 个中文 live Demo 为统计单位，每项关联双语实现。映射、运行检查与严格验收分别记录，完成标准见[文档流程](workflow.md)。

## 当前进度与口径

| 工作               | 当前进度       | 剩余                           | 下一步                                 |
| ------------------ | -------------- | ------------------------------ | -------------------------------------- |
| 双语示例映射       | 859/859        | 0 项待映射                     | 章节/API/迁移收尾审阅                  |
| 严格视觉与行为验收 | 7/859 当前有效 | 346 项待恢复；506 项未首次验收 | 先核对 Breadcrumb、Switch 等低成本候选 |

以上为 2026-09-13 最近正式验收后的快照；当前有效性以 [coverage.json](coverage.json) 和工具核验为准。沿用 `unmapped`、`implemented-awaiting-parity`、`accepted`，不新增状态系统。

最近完成 Table 第 31–37 项：新增 7 项、56 条双语/明暗/LTR/RTL 正式用例，22 条代表诊断通过。范围与固定行为见 [Table 第三批前提](table-3-acceptance.md)，有效结果见[正式证据](evidence/table-3.json)。完整报告按 6 片无损归档，每片不超过 32 MiB，重组后保留全部附件和原校验。

Table 第三批对齐公开行为、接入真实 react-window，并更新共享锁文件、参考配置和证据工具，历史 45 批、346 项因此失效。旧报告与原指纹保持不变，未恢复历史验收；累计取得过证据的 353 项不等于当前有效进度。

## 选批顺序

按[并行验收技能](../../.agents/skills/parallel-documentation-acceptance/SKILL.md)选批：复用有效证据，跨分类筛选依赖与实现已就绪的候选，再按具体示例及完整批次的实际复杂度由低到高推进；成本相近时才沿用 `coverage.json` 的固定文档顺序。高复杂度项提前须有用户指定或已证实的后续依赖。未就绪项保留缺口，不阻塞独立候选。

下表仅为未首次验收的分类库存，不表示执行优先级，也不含历史待恢复项。已有批次沿用其完整矩阵，不为缩短运行删例。

| 分类                                                                                           | 未首次验收数量 |
| ---------------------------------------------------------------------------------------------- | -------------: |
| 基础、Accessibility、基础反馈                                                                  |              0 |
| 展示类                                                                                         |             21 |
| 导航类                                                                                         |             87 |
| 输入类（含 Transfer）                                                                          |            312 |
| 工具类（DragMove、HotKeys）                                                                    |              9 |
| 其他 Plus（CodeHighlight、JsonViewer、MarkdownRender、AudioPlayer、VideoPlayer、Lottie、Chat） |             41 |
| AI（AIChatDialogue、AIChatInput、Sidebar、AIComponent）                                        |             36 |

当前具体边界：

- 优先核对的静态候选为 Breadcrumb 第 1–4 项（Basic、Icons、Size、Separator）、Switch 第 1–3 项（Basic、Size、Disabled）和 Checkbox 第 1–4 项（Basic、DefaultChecked、Extra、Disabled）。这 11 项均未取得过正式证据，双语 SFC 已存在，不属于已有批次；代表实现以本地组件和直接交互为主，未见对 List/Table 待补滚动、拖拽能力的直接依赖。此处仅记录候选池，不声明已就绪或预定批次；选批时仍需核对固定片段、双语语义、依赖及键盘/RTL 等实际行为，形成批次后执行完整矩阵。当前没有这些候选的运行耗时数据。
- Table 第 31–37 项当前有效；第 1–28、30 项保留历史证据、待恢复；第 29 项 DragSort 的真实第三方交互仍待对齐。展开渲染对象额外列属性的公开契约缺口仍见[对齐记录](../components/table/alignment.md)。
- List 第 7 项 ScrollLoad、第 8 项 Virtualized 保留为待就绪项，不再作为默认队首。参考需分别接入固定锁文件的真实 react-infinite-scroller@1.2.6、react-virtualized@9.22.5；react-window 不覆盖这两项。下述阻塞证据未变时直接复用，不重复试探；恢复推进时先核对依赖准备的共享输入影响，再执行完整双语/主题/方向矩阵。
- ScrollLoad 尚未就绪：固定双语片段在函数式 setDataSource 内递增 countRef，并调用 setLoading/setHasMore。2026-09-13 接入真实依赖后的中文只读观察得到每次追加 5 行、标题段依次为 0–4、10–14、20–24……90–94，按钮在第 2、4……10 次请求完成后出现，空结果请求后共 50 行。该观察暴露了计数与按钮门槛差异，不是有效验收：代表诊断未通过，独立观察也在耗尽后的再次加载断言失败。继续前须解释该运行序列与固定更新逻辑的关系，并核对双语及构建模式，不能直接用预想的连续 100 行、每四批门槛改写断言，或把一次观察提升为已确认契约。Vue 另有双语文案、Spinner 层级与按钮区域高度差异。
- Virtualized 已有真实窗口计算与异步加载，并非全量渲染或按钮模拟；但 Vue 渲染 Skeleton 而固定片段在缺数据时返回空行，加载区间/并发策略、List.Item 定位层级、AutoSizer 测宽及双语正文仍需局部对齐。先验证首批、连续/快速跳滚、返回已加载区间、末行与宽度变化，不扩建公开 List API。
- 本次限时轮未新增有效验收。累计定位成本超出选批判断后停止追加修复，未启动正式矩阵；临时实现、新批次和共享依赖改动已撤回，只保留队首缺口。Table 第三批仍复用原 7 项、56 条正式证据，未改旧指纹、未恢复历史批次。诊断默认态和首批加载态的通过结果只用于定位，不计入 accepted；临时材料保留于 ignored 的 apps/docs/.data/documentation-smoke/list-2/。范围控制生效，但本轮有多次测试和临时配置纠错，不能据此认定技能修改已提高验收效率。
- List 第 9 项与 Table 第 29 项 DragSort 单独对齐真实 dnd-kit 的传感器、垂直约束及自动滚动，不用静态或原生拖放替代。
- Transfer 需核对原生拖放、删除按钮和自定义空态；UserGuide 需核对封面适配、定位/高亮/受控/动效；DragMove 需核对双语窄容器与 right 定位；HotKeys 需核对默认快捷键和局部监听。

已有映射不替代示例运行、语义审阅或完整正式矩阵。共享改动后复用仍有效的历史证据，失效项如实计数；新批次不自动恢复全部历史 accepted，恢复任务与发布候选再覆盖所需完整矩阵。

## 收尾与发布

完成无 Demo 指南、全量章节/API/迁移审阅、站点壳视觉、搜索/链接/编辑器回归及 Nuxt/REPL 传递依赖许可审计。全部示例最终仍需严格验收，再按[发布审计](../release-audit-1.0.md)完成候选门禁与实际上线验证。
