import Tooltip from './Tooltip.vue';

export { Tooltip };
export { TOOLTIP_POSITIONS, TOOLTIP_TRIGGERS } from './types';
export type {
  TooltipArrowBounding,
  TooltipEmits,
  TooltipExposed,
  TooltipInitialFocusRef,
  TooltipMargin,
  TooltipPosition,
  TooltipProps,
  TooltipSlots,
  TooltipSpacing,
  TooltipTrigger,
} from './types';

export default Tooltip;
