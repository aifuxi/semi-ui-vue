import Popover from './Popover.vue';

export { Popover };

export type {
  PopoverArrowBounding,
  PopoverArrowStyle,
  PopoverEmits,
  PopoverExposed,
  PopoverMargin,
  PopoverPosition,
  PopoverProps,
  PopoverSlots,
  PopoverSpacing,
  PopoverTrigger,
} from './types';

export default Popover;
