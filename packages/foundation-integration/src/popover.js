// Keep Popover runtime constants behind the private pinned Foundation boundary.
export {
  cssClasses as popoverCssClasses,
  numbers as popoverNumbers,
  strings as popoverStrings,
} from '../../../vendor/semi-design/packages/semi-foundation/popover/constants';
