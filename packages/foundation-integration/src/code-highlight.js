// Keep the pinned Prism highlighting and line-number state machine behind this private boundary.
// @ts-expect-error -- the pinned vendor source is intentionally compiled only through this private boundary.
export { default as CodeHighlightFoundation } from '../../../vendor/semi-design/packages/semi-foundation/codeHighlight/index';
