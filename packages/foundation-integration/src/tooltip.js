// Keep Tooltip placement, overflow, trigger, delay and focus behavior behind the
// private pinned Foundation boundary.
export { default as TooltipFoundation } from '../../../vendor/semi-design/packages/semi-foundation/tooltip/foundation';
