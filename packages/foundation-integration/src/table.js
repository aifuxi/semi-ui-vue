// Keep the pinned Table constants behind the private Foundation boundary.
// The Vue adapter consumes these values without importing vendor/** directly.
export {
  cssClasses as tableCssClasses,
  numbers as tableNumbers,
  strings as tableStrings,
} from '../../../vendor/semi-design/packages/semi-foundation/table/constants';
