// Keep the pinned Highlight matching algorithm behind the private Foundation boundary.
// @ts-expect-error -- the pinned vendor source is intentionally compiled only through this private boundary.
export { default as HighlightFoundation } from '../../../vendor/semi-design/packages/semi-foundation/highlight/foundation';
